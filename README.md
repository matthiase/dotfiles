# dotfiles

```
$ git clone git@github.com:matthiase/dotfiles.git && cd dotfiles
$ ./setup.sh
```

## Homebrew
## iTerm2
## Hack Nerd Font
To change your font used in your iTerm2 profile, open the preferences and navigating to the font section of your profile.
```
iTerm2 -> Preferences
Profiles -> Text -> Font -> Change Font
```
Select the font Hack Regular Nerd Font Complete and adjust the size if your want too. Also check the box for `Use a different font for non-ASCII text` and select the font again. It should be displaying the new font and icons in the prompt.

## Zsh
## Powerlevel9K
